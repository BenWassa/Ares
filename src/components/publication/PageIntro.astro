---
interface Props {
  title: string;
  description: string;
  id?: string;
}
const { title, description, id } = Astro.props;
---
<header class="page-intro" id={id}>
  <h1>{title}</h1>
  <p class="page-deck">{description}</p>
</header>
